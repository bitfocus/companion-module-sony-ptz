// Shared action defaults. Re-exported from ./index.js for presets.ts, which references these
// for the PTZ move/zoom speed and step rotary presets — keep the values stable.
export const DEFAULT_PTZ_MOVE_SPEED = 8
export const DEFAULT_PTZ_ZOOM_SPEED = 8000
export const DEFAULT_PTZ_STEP = 5
